<IfModule mod_rewrite.c>
  RewriteEngine On
  # Si el archivo o carpeta no existe, redirigir todo a index.html
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . index.html [L]
</IfModule>
